# Copy these into your hosting provider's environment variables for stable push subscriptions.
# If omitted during local development, server.js generates and stores keys in the gitignored vapid-keys.json file.
VAPID_EMAIL=mailto:your-email@example.com
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
